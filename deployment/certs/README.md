Place your TLS certificate files here when using the optional nginx proxy profile.

Expected filenames:
- fullchain.pem
- privkey.pem

These files are gitignored and should come from your certificate provider.